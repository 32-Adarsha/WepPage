import { DynamicLoaderDirective } from './dynamic-loader.directive';

describe('DynamicLoaderDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new DynamicLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
